/* 
// Square every number in the array

let numbers = [1, 2, 3, 4, 5];
let squared = numbers.map((num) => num ** 2);
console.log(numbers);
console.log(squared);
*/

// What is happening in this code and why?
[[1, 2], [3, 4]].forEach(arr => console.log(arr[0]));
// 1
// 3
// => undefined

